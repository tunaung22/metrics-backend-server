@page
@model Metrics.Web.Pages.Manage.Submissions.Departments.Scores.IndexModel
@{
    ViewData["Title"] = "Score Submission Status";
}


<div class="sectionTitle">
    <div class="d-flex align-items-center">
        <h4>Department Score Submissions Status</h4>
        <span class="justify-content-end gap-2"></span>
    </div>
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-page="/Dashboard/Index">Dashboard</a></li>
            <li class="breadcrumb-item"><a asp-page="/Reports/Index">Reports</a></li>
            <li class="breadcrumb-item active">Department Score Submissions</li>
        </ol>
    </nav>
</div>

<div class="container">


    <div class="my-4 pb-4">
        @* ========================================================================== *@
        @if (!ModelState.IsValid)
        {
            <div class="form bg-light p-4">
                <div class="alert alert-danger">
                    <h4>There were some errors:</h4>
                    <ul>
                        @foreach (var error in ModelState.Values.SelectMany(v => v.Errors))
                        {
                            <li>@error.ErrorMessage</li>
                        }
                    </ul>
                </div>
            </div>
        }

        @* =============== Filter Bar =============================================== *@
        @* <div id="filter-bar" class="d-flex bg-light p-4 mt-2"><div class="col-sm-4 mx-2"></div></div> *@

        @* ================ Table =================================================== *@
        <div class="row">
            <div class="col-auto">
                <div class="card my-4">
                    @* <div class="card-header">
                        <strong>Reports</strong>
                    </div> *@
                    <ul class="list-group list-group-flush">
                        @* <li class="list-group-item p-2">
                            <a class="" asp-page="/Reports/Submissions/Departments/Kpi/Index">
                                <i class="bi bi-clipboard2-data"> </i>List of Department Score Submissions (KPI)</a></li>
                        <li class="list-group-item p-2">
                            <a class="" asp-page="/Reports/Submissions/Departments/KeyKpi/Index">
                                <i class="bi bi-clipboard2-data"> </i>Metric Score Submissions (Key KPI)</a></li>
                        <li class="list-group-item p-2">
                            <a class="" asp-page="/Reports/Submissions/Departments/KeyKpi/Index">
                                <i class="bi bi-clipboard2-data"> </i></a></li>
                        <li class="list-group-item p-2">
                            <a class="" asp-page="/Reports/Submissions/Departments/KeyKpi/Index">
                                <i class="bi bi-clipboard2-data"> </i></a></li> *@
                    </ul>
                </div>
            </div>
        </div>
        @* <div class="bg-light p-4">
            <table id="departmentsTable" class="table">
                <thead>
                    <tr>
                        <th>Period</th>
                        <th>Submissions</th>
                    </tr>
                </thead>
                <tbody>
                </tbody>
            </table>
        </div> *@
    </div>
</div>