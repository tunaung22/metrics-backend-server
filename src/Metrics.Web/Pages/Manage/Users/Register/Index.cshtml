@page
@model Metrics.Web.Pages.Manage.Users.Register.IndexModel
@{
    ViewData["Title"] = "Register new Account";
}

@* @if (!ModelState.IsValid)
{
    <div class="form bg-light p-4">
        <div class="alert alert-danger">
            <h5>There were some errors in the form.</h5>
            <ul>
                <li>@ModelState.Values.SelectMany(v => v.Errors).First().ErrorMessage</li>
            </ul>
        </div>
    </div>
} *@



<div class="container">

    <div class="d-flex align-items-center my-2">
        <h4 class="flex-grow-1">Register new user</h4>
    </div>

    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/Dashboard/Index">Dashboard</a></li>
            <li class="breadcrumb-item"><a href="/Manage/Users/Index">Users</a></li>
            <li class="breadcrumb-item active">Register</li>
        </ol>
    </nav>


    <div class="col-lg-12 col-xl-10 pt-2 mx-auto">
        <form id="registerForm" method="post" asp-page-handler="">

            <div asp-validation-summary="ModelOnly" class="alert alert-danger text-danger" role="alert"></div>

            <div class="row section__authorization pt-2 pb-4 mb-2 rounded-2"> <!-- Authorities -->
                @* <div class="row mb-4"> *@
                    @* <div class="col-xs-12 col-sm-12 col-md-12"> *@
                        <div class="col-xs-12 col-sm-12 col-md-4"> <!-- User Group -->
                            <label asp-for="FormInput.UserTitleId" class="form-label">Position</label>
                            <select asp-for="FormInput.UserTitleId" 
                                    asp-items="Model.UserTitleListItems"
                                    class="form-select" 
                                    aria-label="Select Department">
                                <option value="" selected hidden></option>
                            </select>
                            <div><span asp-validation-for="FormInput.UserTitleId" class="text-danger"></span></div>
                        </div>
                        <div class="col-xs-12 col-sm-12 col-md-4"> <!-- Department -->
                            <label asp-for="FormInput.DepartmentId" class="form-label">Department</label>
                            <select asp-for="FormInput.DepartmentId" 
                                    asp-items="Model.DepartmentListItems"
                                    class="form-select" 
                                    aria-label="Select Department">
                                <option value="" selected hidden></option>
                            </select>
                            <div><span asp-validation-for="FormInput.DepartmentId" class="text-danger"></span></div>
                        </div>
                        <div class="col-xs-12 col-sm-12 col-md-4"> <!-- Authority -->
                            <label asp-for="FormInput.RoleId" class="form-label">Authority</label>
                            <i class="bi bi-info-circle" 
                                data-bs-toggle="tooltip" 
                                data-bs-placement="top" 
                                data-bs-title="Authorization: Whether the user can access admin features or not."></i>
                            <select asp-for="FormInput.RoleId" 
                                    asp-items="Model.RoleListItems"
                                    class="form-select" 
                                    aria-label="Select Role">
                                <option value="" selected hidden></option>
                            </select>
                            <div><span asp-validation-for="FormInput.UserTitleId" class="text-danger"></span></div>
                        </div>
                    @* </div> *@
                        @* <label asp-for="FormInput.RoleId" class="form-label">User Role</label>
                        <select asp-for="FormInput.RoleId" 
                                asp-items="Model.RoleListItems"
                                class="form-select" 
                                aria-label="Select Role">
                            <option value="" selected hidden></option>
                        </select>
                        <div><span asp-validation-for="FormInput.RoleId" class="text-danger"></span></div> *@


                        @* <label asp-for="FormInput.RoleIds" class="form-label">Roles</label> *@
                        @* @foreach (var role in Model.AvaiableRoles)
                        { <div>
                                <input type="checkbox" 
                                    name="SelectedRoleIds" 
                                    value="@role.Id" 
                                    checked="@Model.SelectedRoleIds.Contains(role.Id)" />
                                <label>@role.Name?.ToUpper()</label>
                            </div> }
                        <div><span asp-validation-for="FormInput.RoleIds" class="text-danger"></span></div> *@
                @* </div> *@
            </div>

            <div class="row section__credentials pt-2 pb-4 mb-2 rounded-2"> <!-- Credentials -->
                <div class="row"> <!-- username / email -->
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6"> <!-- username -->
                        <label asp-for="FormInput.UserName" class="form-label">Username</label>
                        <input asp-for="FormInput.UserName"
                            id="input__userName"
                            class="form-control" />
                        <div><span asp-validation-for="FormInput.UserName" class="text-danger"></span></div>
                    </div>
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">  <!-- email -->
                        <label asp-for="FormInput.Email" class="form-label">Email</label>
                        <input asp-for="FormInput.Email" class="form-control" />
                        <div><span asp-validation-for="FormInput.Email" class="text-danger"></span></div>
                    </div>
                </div>
                <div class="row"> <!-- password 1 -->
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6"> 
                        <label asp-for="FormInput.Password" class="form-label">Password</label>
                        <input asp-for="FormInput.Password" 
                                class="form-control" 
                                autocomplete="new-password" 
                                aria-required="true"/>
                        <div><span asp-validation-for="FormInput.Password" class="text-danger"></span></div>
                    </div>
                </div>
                <div class="row"> <!-- password 2 -->
                    <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                        <label asp-for="FormInput.ConfirmPassword" class="form-label">Confirm Password</label>
                        <input asp-for="FormInput.ConfirmPassword" 
                                class="form-control" 
                                autocomplete="new-password" 
                                aria-required="true" />
                        <div><span asp-validation-for="FormInput.ConfirmPassword" class="text-danger"></span></div>
                    </div>
                </div>
            </div>

            <div class="row section__userInfo p-2 my-2 rounded-2"> 
                <div class="col">
                    <div class="row mb-4">
                        <div class="col-sm-12 col-md-12 col-lg-6">
                            <label asp-for="FormInput.UserCode" class="form-label">Staff ID</label>
                            <div class="input-group">
                                <input asp-for="FormInput.UserCode"
                                    id="input__userCode" 
                                    class="form-control" />
                                <button id="generateId" 
                                        class="btn btn-outline-success" 
                                        type="button">Generate</button>
                            </div>
                            <div><span asp-validation-for="FormInput.UserCode" class="text-danger"></span></div>
                        </div>
                        <div class="col-sm-12 col-md-12 col-lg-6">
                            <label asp-for="FormInput.FullName" class="form-label">Full Name</label>
                            <input asp-for="FormInput.FullName" class="form-control" />
                            <div><span asp-validation-for="FormInput.FullName" class="text-danger"></span></div>
                        </div>
                    </div>

                    <div class="row mb-4">
                        <div class="col-sm-12 col-md-6">
                            <label asp-for="FormInput.Address" class="form-label">Address</label>
                            <textarea asp-for="FormInput.Address" class="form-control"></textarea>
                            <div><span asp-validation-for="FormInput.Address" class="text-danger"></span></div>
                        </div>
                        <div class="col-sm-12 col-md-6">
                            <label asp-for="FormInput.PhoneNumber" class="form-label">Phone</label>
                            <textarea asp-for="FormInput.PhoneNumber" class="form-control"></textarea>
                            <div><span asp-validation-for="FormInput.PhoneNumber" class="text-danger"></span></div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row section__formAction my-4">
                <div class="d-flex justify-content-end">
                    <button id="registerSubmit" type="submit" class="btn btn btn-primary mb-3">
                        <i class="bi bi-check-circle"> </i>Register
                    </button>
                </div>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @* <partial name="_ValidationScriptsPartial" /> *@
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }

    <script>
        $(document).ready(function() {
            $("#input__userName").on("input", function() {
                var start = this.selectionStart;
                var end = this.selectionEnd; // Store the current selection range
                $(this).val($(this).val().toLowerCase()); // Convert text to lowercase
                this.setSelectionRange(start, end); // Restore the selection range
            });

            $("#input__userCode").on("input", function() {
                var start = this.selectionStart; // Store the current selection range
                var end = this.selectionEnd;
                $(this).val($(this).val().replace(/\s+/g, '-')); // space to '-'
                // $(this).val($(this).val().toLowerCase()); // Convert text to lowercase
                this.setSelectionRange(start, end); // Restore the selection range
            });

            $("#generateId").on("click", function() {
                // https://stackoverflow.com/questions/105034/how-do-i-create-a-guid-uuid#2117523
                const ho = (n, p) => n.toString(16).padStart(p, 0); /// Return the hexadecimal text representation of number `n`, padded with zeroes to be of length `p`; e.g. `ho(13, 2)` returns `"0d"`
                const data = crypto.getRandomValues(new Uint8Array(16)); /// Fill a buffer with random bits
                data[6] = (data[6] & 0xf) | 0x40; /// Patch the 6th byte to reflect a version 4 UUID
                data[8] = (data[8] & 0x3f) | 0x80; /// Patch the 8th byte to reflect a variant 1 UUID (version 4 UUIDs are)
                const view = new DataView(data.buffer); /// Create a view backed by the 16-byte buffer
                var generatedEmployeeCode = `${ho(view.getUint32(0), 8)}-${ho(view.getUint16(4), 4)}-${ho(view.getUint16(6), 4)}-${ho(view.getUint16(8), 4)}-${ho(view.getUint32(10), 8)}${ho(view.getUint16(14), 4)}`; /// Compile the canonical textual form from the array data
                $("#input__userCode").val(generatedEmployeeCode);
            });
        });
    </script>
}

@section Styles {
    <style>
        .section__authorization, .section__credentials, .section__userInfo {
            /* background: #e2e2e2; */
            background: #00000012;
        }

        .section__formAction {
        }
    </style>
}